# CLAUDE.md

このリポジトリで作業する Claude Code 向けの運用ルール。

## 作業環境の前提（最重要）

クリスは **iOS / Web のこのアプリを入口**にして、**MacBook 上で Claude Code を動かして作業させたい**。
クラウドセッションだけで完結させようとせず、**Mac 側のリソースが必要なときは必ず Mac に繋ぎに行くこと。**

### Mac への繋ぎ方

1. `mcp__Claude_Code_Remote__list_environments` でブリッジ環境を確認する
   - 現時点: `env_01LyzQv2G63xwoPxS9dQ1Ekk` / `MacBook-Pro-8:my-ai-agent:e09a` (`kind: bridge`)
   - ID は変わりうるので、決め打ちせず毎回 `list_environments` で引くこと
2. `mcp__Claude_Code_Remote__create_session` に **その `environment_id` を渡して** Mac 上にセッションを立てる
3. `get_session` で状態を見る。結果は Mac セッションの画面に出させるか、`SendMessage` で呼び出し元に返させる

### 引っかかりやすい罠

- **`ListAgents` にブリッジセッションは出ない。** あれは「今自分が動いているマシン上の」セッションしか見ない。
  クラウドコンテナで実行中なら当然ゼロ件になる。**これを根拠に「Mac と繋がっていない」と結論するのは誤り。**
  アカウント全体は `mcp__Claude_Code_Remote__list_sessions` / `list_environments` で見る。
- Mac 側セッションは**別の権限境界**で動く。機微情報（口座番号・本名・住所など）の取り扱いは
  そのセッションが本人確認を求めて止まることがある。**迂回しようとせず、クリスに確認を回すこと。**

## クラウドセッションから届かないもの

以下はクラウド側からはアクセス不可。必要なら Mac 経由か、チャットへの直接添付で受け取る。

- **Gmail** — コネクタ未接続（接続済みは Google カレンダー / Notion / Expedia のみ）
- **Google ドキュメント / ドライブ** — コネクタ未接続 **かつ** `docs.google.com` がネットワーク egress でブロック済み。
  リンク共有 URL を直接叩いても `EGRESS_BLOCKED` で弾かれる
- **Mac ローカルのファイル** — このリポジトリは GitHub からの clone であって Mac のフォルダではない

## 経理・機微情報の扱い

- `.gitignore` の `61*/` は「本社（経営・経理・契約）フォルダ＝ローカル専用・push しない」。**この方針を破らないこと。**
  経理フォルダは GitHub に存在しないので clone にも来ない。無いのが正常。
- **請求書・口座番号・住所・本名・インボイス登録番号を commit しない。** 成果物はファイル直渡し（`SendUserFile`）か Mac ローカル保存で返す。
- `site/` は **sixten.jp として公開される**。機微なものを置かない。

## 請求書について

- 個人名義の案件は源泉徴収（登壇・出演等は原則 10.21%、100万円超の部分は 20.42%）の扱いを必ず確認する。
- 発注書ベースの請求は、**発注書番号（PO番号）を請求書に明記**する。先方によってはベンダーコード等の併記を求められる。
- 書式・固定情報（氏名／住所／振込先／インボイス登録番号／採番ルール）は Mac ローカルの過去請求書が一次情報。
